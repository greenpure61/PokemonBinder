// Extends Vitest's `expect` with jest-dom matchers (toBeInTheDocument, etc.)
// for the component tests added in later phases. Harmless for pure-logic tests.
import "@testing-library/jest-dom/vitest";
